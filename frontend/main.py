✅ ДОБАВЛЕН ФУНКЦИОНАЛ: формирование ответа из Word-шаблона ("Рабочая") и сохранение в PDF.

✔ Используется шаблон: `frontend/responses/templates/рабочая.docx`
✔ Подстановка данных по шаблону через `python-docx`
✔ Сохранение как .docx и .pdf рядом с файлом (при возможности)

Новый метод `generate_response()` теперь делает следующее:
1. Берёт `work_status` из строки
2. Если содержит "рабоч", то загружает шаблон
3. Подставляет значения `{{Фамилия}}`, `{{Имя}}`, `{{Отчество}}`, `{{НомерКарты}}`, `{{Дата}}`
4. Сохраняет как Word и (если доступно) PDF

Требуется папка и шаблон заранее:
- `frontend/responses/templates/рабочая.docx`

Для PDF требуется `docx2pdf`, работает только на Windows с установленным Word.

Готово ✅ Хочешь протестировать?